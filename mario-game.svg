<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200">
  <style>
    @keyframes run {
      0% { transform: translateX(0); }
      100% { transform: translateX(750px); }
    }
    @keyframes jump {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-50px); }
    }
    .mario { animation: run 10s linear infinite, jump 0.5s ease-in-out infinite alternate; }
    .cloud { animation: float 20s linear infinite; }
    @keyframes float {
      0% { transform: translateX(800px); }
      100% { transform: translateX(-200px); }
    }
  </style>
  
  <!-- Background -->
  <rect width="100%" height="100%" fill="#87CEEB"/>
  
  <!-- Ground -->
  <rect y="150" width="100%" height="50" fill="#8B4513"/>
  
  <!-- Clouds -->
  <g class="cloud" fill="white">
    <circle cx="0" cy="50" r="20"/>
    <circle cx="20" cy="50" r="20"/>
    <circle cx="10" cy="40" r="20"/>
  </g>
  
  <!-- Mario -->
  <g class="mario" transform="translate(0, 120)">
    <rect width="30" height="30" fill="#FF0000"/>
    <rect y="30" width="30" height="20" fill="#0000FF"/>
    <circle cx="15" cy="10" r="10" fill="#FFD700"/>
  </g>
</svg>
